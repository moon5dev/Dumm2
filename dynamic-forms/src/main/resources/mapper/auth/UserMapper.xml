<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.moon5.dynamicforms.auth.UserMapper">

	<select id="findByLoginId" resultType="dev.moon5.dynamicforms.auth.User">
		SELECT id, login_id, password_hash, created_at
		FROM users
		WHERE login_id = #{loginId}
	</select>

	<insert id="insert" parameterType="dev.moon5.dynamicforms.auth.User"
			useGeneratedKeys="true" keyProperty="id">
		INSERT INTO users (login_id, password_hash, created_at)
		VALUES (#{loginId}, #{passwordHash}, #{createdAt})
	</insert>

</mapper>
